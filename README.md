# mdisk-caches
CLI tool to set up and manage RAM disk / tmpfs caches on macOS and Ubuntu, with auto-detection, recommendations, and 3rd-party tool redirection (Maven/Gradle/npm/Docker BuildKit/TMPDIR).
